services:
  off_crm:
    build: .
    ports:
      - "127.0.0.1:8766:8766"
    environment:
      OFF_CRM_LOCAL_API_TOKEN: "${OFF_CRM_LOCAL_API_TOKEN:?Set a random token with at least 32 characters}"
      OFF_CRM_OWN_EMAIL: "${OFF_CRM_OWN_EMAIL:-}"
      OPENAI_API_KEY: "${OPENAI_API_KEY:-}"
      ANTHROPIC_API_KEY: "${ANTHROPIC_API_KEY:-}"
    volumes:
      - ./local_data:/app/local_data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8766/health/ready', timeout=3)"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
